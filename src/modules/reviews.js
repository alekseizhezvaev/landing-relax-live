"use strict"
import sliderUniversal from "./sliderUniversal"

const reviews = () => {
  const reviewsSlider = ".reviews-slider"
  const reviewsRight = "reviews-arrow_right"
  const reviewsLeft = "reviews-arrow_left"

  sliderUniversal(reviewsSlider, reviewsRight, reviewsLeft)
}

export default reviews
